# Copie este arquivo para ".env" (sem o .example) e preencha com seus dados reais.
# O .env de verdade NUNCA deve ser commitado no Git — é por isso que existe
# um .env.example: ele mostra a estrutura sem expor nenhuma senha real.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha_aqui
DB_NAME=HumanData_RH_Solutions